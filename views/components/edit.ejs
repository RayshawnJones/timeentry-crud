<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Time Entry</title>
</head>
<body>
    <h1>Edit Time Entry</h1>
    <form action="/time/<%= entry._id %>?_method=PUT" method="POST">
        <label for="date">Date:</label>
        <input type="date" id="date" name="date" value="<%= entry.date.toISOString().split('T')[0] %>" required><br>
        <label for="hours">Hours:</label>
        <input type="number" id="hours" name="hours" value="<%= entry.hours %>" required><br>
        <label for="description">Description:</label>
        <textarea id="description" name="description" required><%= entry.description %></textarea><br>
        <button type="submit">Update Entry</button>
    </form>
    <a href="/">Back to Home</a>
</body>
</html>
